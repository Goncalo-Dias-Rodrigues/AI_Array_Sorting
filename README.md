# Objective

Program that computes the minimum-cost sequence of swaps that converts an initial integer array into a target array,
where each elementary swap has a cost depending on the parity of the swapped values:
even ↔ even ⇒ cost = 2
even ↔ odd ⇒ cost = 11
odd ↔ odd ⇒ cost = 20
